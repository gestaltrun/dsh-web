# AUTO-GENERATED by scripts/aggregate.mjs — do not edit
# This file is derived from the aggregate.yml manifest in this package;
# edit aggregate.yml and rerun `node scripts/aggregate.mjs`.
# Rows are namespaced web-ui-* so the bundle can coexist with standalone installs.
# Family rows mount the aggregate's per-family subpath exports so the
# official plugin inventory renders one distinct "web-all/<family>" title per row.

# from self
- insert:
    - id: web-ui-compat
      name: '@gestaltrun/dsh-web-all'

# from ../dsh-web-settings
- insert:
    - id: web-ui-settings
      name: '@gestaltrun/dsh-web-all/settings'
      config:
        plugin: '@gestaltrun/dsh-client-ui-web-ui-settings'

# from ../dsh-plugin-manager
- insert:
    - id: web-ui-plugin-manager
      name: '@gestaltrun/dsh-web-all/plugin-manager'
      config:
        plugin: '@gestaltrun/dsh-client-ui-plugin-manager'

# from ../dsh-community-plugins
- insert:
    - id: web-ui-community-plugins
      name: '@gestaltrun/dsh-web-all/community-plugins'
      config:
        plugin: '@gestaltrun/dsh-client-ui-community-plugins'

# from ../dsh-market
- insert:
    - id: web-ui-market
      name: '@gestaltrun/dsh-web-all/market'
      config:
        plugin: '@gestaltrun/dsh-client-ui-market'

# from ../dsh-task-board
- insert:
    - id: web-ui-task-board
      name: '@gestaltrun/dsh-web-all/task-board'
      config:
        plugin: '@gestaltrun/dsh-client-ui-task-board'

# from ../dsh-git-graph
- insert:
    - id: web-ui-git-graph
      name: '@gestaltrun/dsh-web-all/git-graph'
      config:
        plugin: '@gestaltrun/dsh-client-ui-git-graph'

# from ../dsh-remote-web-ui
- insert:
    - id: web-ui-remote-web-ui
      name: '@gestaltrun/dsh-web-all/remote-web-ui'
      config:
        plugin: '@gestaltrun/dsh-remote-web-ui'

# from ../dsh-pet
- insert:
    - id: web-ui-pet
      name: '@gestaltrun/dsh-web-all/pet'
      config:
        plugin: '@gestaltrun/dsh-pet'

# from ../dsh-ssh
- insert:
    - id: web-ui-ssh
      name: '@gestaltrun/dsh-web-all/ssh'
      config:
        plugin: '@gestaltrun/dsh-ssh'

# from ../dsh-tool-describe-image
- insert:
    - id: web-ui-describe-image
      name: '@gestaltrun/dsh-web-all/describe-image'
      config:
        plugin: '@gestaltrun/dsh-tool-describe-image'

# from ../dsh-liangshen
- insert:
    - id: web-ui-liangshen
      name: '@gestaltrun/dsh-web-all/liangshen'
      config:
        plugin: '@gestaltrun/dsh-liangshen'

# from ../dsh-skill-explorer
- insert:
    - id: web-ui-skill-explorer
      name: '@gestaltrun/dsh-web-all/skill-explorer'
      config:
        plugin: '@gestaltrun/dsh-client-ui-skill-explorer'

# from ../dsh-doctor
- insert:
    - id: web-ui-doctor
      name: '@gestaltrun/dsh-web-all/doctor'
      config:
        plugin: '@gestaltrun/dsh-doctor'

# from ../dsh-usage
- insert:
    - id: web-ui-usage
      name: '@gestaltrun/dsh-web-all/usage'
      config:
        plugin: '@gestaltrun/dsh-usage'

# from ../dsh-session-archive
- insert:
    - id: web-ui-session-archive
      name: '@gestaltrun/dsh-web-all/session-archive'
      config:
        plugin: '@gestaltrun/dsh-session-archive'

# from ../dsh-model-capabilities
- insert:
    - id: web-ui-model-capabilities
      name: '@gestaltrun/dsh-web-all/model-capabilities'
      config:
        plugin: '@gestaltrun/dsh-client-ui-model-capabilities'

# from ../dsh-preset-center
- insert:
    - id: web-ui-preset-center
      name: '@gestaltrun/dsh-web-all/preset-center'
      config:
        plugin: '@gestaltrun/dsh-client-ui-preset-center'

# from ../skins/skin-center
- insert:
    - id: web-ui-skin-center
      name: '@gestaltrun/dsh-web-all/skin-center'
      config:
        plugin: '@gestaltrun/dsh-client-ui-skin-center'

# from ../dsh-i18n
- insert:
    - id: web-ui-i18n
      name: '@gestaltrun/dsh-i18n'

# inactive by default (opt-in rows): these ship disabled; each is enabled per row in the
# plugin manager, whose enable writes a user-layer "disabled: false" override that wins.
- id: web-ui-pet
  disabled: true
- id: web-ui-skin-center
  disabled: true
- id: web-ui-describe-image
  disabled: true
- id: web-ui-liangshen
  disabled: true
- id: web-ui-skill-explorer
  disabled: true
- id: web-ui-doctor
  disabled: true

# external bundle: @gestaltrun/dsh-better-sidebar

# from external bundle @gestaltrun/dsh-better-sidebar (insert row web-ui-better-sidebar)
- insert:
    - id: web-ui-better-sidebar
      name: '@gestaltrun/dsh-better-sidebar'
